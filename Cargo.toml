[package]
name = "office_holdem"
version = "0.1.0"
edition = "2024"

[dependencies]
eframe = { version = "0.32", default-features = false, features = ["default_fonts", "glow"] }
rand = "0.9"

[profile.release]
opt-level = 3
lto = "thin"
strip = true

